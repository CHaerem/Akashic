/**
 * Custom hook for Mapbox map initialization and management
 *
 * This file re-exports from the modular mapbox/ directory.
 * Direct imports from this file are preserved for backwards compatibility.
 *
 * For new code, prefer importing from 'hooks/mapbox':
 * - import { useMapbox } from '../hooks/mapbox'
 */

export * from './mapbox/index';
